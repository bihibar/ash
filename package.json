{
  "name": "ash",
  "version": "1.0.0",
  "private": true,
  "description": "ash — image & video to ASCII/vector pattern generator",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
